package com.example.expensetracker.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Shared palette — deep ink navy with a mint accent.
 */
val Ink = Color(0xFF14181F)          // near-black navy — primary surfaces / text
val InkSoft = Color(0xFF232A35)      // raised cards on dark, or secondary text on light
val Mint = Color(0xFF3DD68C)         // primary accent — money, positive , CTAs
val MintDeep = Color(0xFF1FA968)     // pressed / stronger accent
val Paper = Color(0xFFF6F7F4)        // light page background
val CardPaper = Color(0xFFFFFFFF)    // raised card on light background
val Muted = Color(0xFF8A93A2)        // muted labels / captions